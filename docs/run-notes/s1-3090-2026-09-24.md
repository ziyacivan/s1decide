# S1 on the 3090 — `s1-3090` — launched 2026-09-23 21:35 UTC

Owner's go on 2026-09-24 (local), config `train/configs/sft_3090_s1.yaml` as proposed in
[`s1-proposal-2026-09-24.md`](s1-proposal-2026-09-24.md). Commit `782dc5a`.

```
uv run task train --cfg train/configs/sft_3090_s1.yaml --detach      # pid 980
```

rank 8 · seq 2048 · lr 2e-5, 112 warmup steps of 3,750, cosine to zero · batch 1 → effective 8 ·
30,000 rows family-balanced, `Score` uniform by row · λ 0.3 · checkpoint (adapter + optimiser +
schedule + position) and case-control `val` evaluation at row 0 and every 5,000 rows · stop rule
as amended by the owner (`train/stop_rule.py`).

## Pre-flight

Unsloth Studio closed and Windows Update paused (confirmed by the owner); `doctor`: 0 fail,
471 MiB foreign VRAM (desktop, browsers), sysmem fallback off, kernels as in the smoke (FLA chunk
+ fused recurrent accelerated, conv1d torch, SDPA); no pending reboot; 515 GB free. `kernels`
package absent from the environment, as in the smoke — recorded in the run's meta with versions.

## Row 0 — done 21:58 UTC

The row-0 evaluation (8,329 rows) took 1,309 s and reproduced the smoke's row 0 exactly on every
metric (`evals.jsonl`), as it should: same untrained model, same fixed slice.

## ETA

**~06:15 UTC on 2026-09-24 (09:15 local)**: training from 21:58 at the smoke's measured 1.376
rows/s is ~6.1 h, plus six more evaluations at ~22 min. Expected peak VRAM ~21.7 GiB, set by the
evaluations.

## Watching

A watcher exits on `DONE`, `FAILED` or `STOPPED`, a heartbeat older than 15 minutes (evaluations
beat every 100 batches), a reboot, or a training interval below half the median rate (intervals
that contain an evaluation are excluded). On an evaluation slowdown the owner's instruction is to
resume from the last checkpoint with `eval_batch_size: 4`; checkpoints carry the optimiser and
schedule state for that.

## Environment notes

- Unsloth logs *"Not offloading embeddings; offloading is unsupported on Windows."* ADR 0002's
  `offload_embedding=True` is therefore a no-op on this machine; it was the same in the smoke, so
  the measured envelope already reflects it.
- From now on the GPU is S1's. CPU-side work (the Laya evaluation) runs in a separate CPU-only
  environment, 6 of 16 threads.
